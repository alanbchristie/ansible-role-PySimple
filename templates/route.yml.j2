---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ name }}
  namespace: {{ namespace }}
  labels:
    app: {{ name }}
    service: {{ name }}
spec:
  port:
    targetPort: pysimple
  to:
    kind: Service
    name: {{ name }}
