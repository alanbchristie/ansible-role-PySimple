---
kind: Service
apiVersion: v1
metadata:
  name: {{ name }}
  namespace: {{ namespace }}
  labels:
    app: {{ name }}
    service: {{ name }}
spec:
  selector:
    app: {{ name }}
  ports:
  - name: http
    port: 8080
    targetPort: http
